create table item(
                     iditem integer primary key,
                     name varchar(50),
                     publisher varchar(50),
                     yearofpublication integer,
                     pages integer,
                     itemweight decimal,
                     description varchar (150),
                     price decimal,
                     image varchar(255)
)